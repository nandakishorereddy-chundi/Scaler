This repo illustrates the LLD And HLD and DS work by me

std functions used:
-->lower_bound()  also look at upper_bound(), binary_search()
-->multimap(), map()
-->vector pointer:  vector<int> *ptr = (vector<int>*)&myvec;
